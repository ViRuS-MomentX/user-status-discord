<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>ru.virus</groupId>
  <artifactId>voice-bridge-bot</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>Voice Bridge Bot</name>
  <description>Следит за голосовыми каналами одного сервера и отдаёт состояние в CustomRP по локальному HTTP.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- 25 требует jdave: шифрование голоса у Discord работает через
         новый нативный интерфейс Java, появившийся только там -->
    <maven.compiler.release>25</maven.compiler.release>
  </properties>

  <dependencies>
    <dependency>
      <groupId>net.dv8tion</groupId>
      <artifactId>JDA</artifactId>
      <version>6.6.0</version>
      <exclusions>
        <!-- Голос нам не нужен: бот только читает состояние каналов -->
        <exclusion>
          <groupId>club.minnced</groupId>
          <artifactId>opus-java</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <!-- Сквозное шифрование голоса (DAVE). Без него Discord закрывает
         голосовое соединение сразу после подключения. -->
    <dependency>
      <groupId>club.minnced</groupId>
      <artifactId>jdave-api</artifactId>
      <version>0.1.8</version>
    </dependency>
    <dependency>
      <groupId>club.minnced</groupId>
      <artifactId>jdave-native-win-x86-64</artifactId>
      <version>0.1.8</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>dev.arbjerg</groupId>
      <artifactId>lavaplayer</artifactId>
      <version>2.2.7</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>2.0.16</version>
    </dependency>
  </dependencies>

  <build>
    <finalName>voice-bridge-bot</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>ru.virus.voicebridge.VoiceBridgeBot</mainClass>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>module-info.class</exclude>
                    <!-- Нативные библиотеки идут под десяток платформ сразу и весят
                         под 40 МБ. Оставляем только Windows, на которой бот и
                         запускают. Для Linux нужно убрать строку с linux-x86-64
                         ниже и вернуть зависимость jdave-native-linux-x86-64. -->
                    <exclude>natives/linux-x86-64/**</exclude>
                    <exclude>natives/darwin/**</exclude>
                    <exclude>natives/linux-aarch32/**</exclude>
                    <exclude>natives/linux-aarch64/**</exclude>
                    <exclude>natives/linux-arm/**</exclude>
                    <exclude>natives/linux-armhf/**</exclude>
                    <exclude>natives/linux-musl-x86-64/**</exclude>
                    <exclude>natives/linux-musl-aarch64/**</exclude>
                    <exclude>natives/linux-x86/**</exclude>
                    <exclude>natives/win-x86/**</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
